// import {logger, Entry, EntryType} from '../lib/logger-typescript/index'

// let entry:Entry  = {
//     message: '这是通过字面量方式定义的对象!!!',
//     type: EntryType.Info
// }

// class EntryImpl implements Entry {
//     constructor(public message: string, public type: EntryType){}
// }

// logger(entry)
// logger(new EntryImpl('这是通过创建new来创建对象的', EntryType.Warning))